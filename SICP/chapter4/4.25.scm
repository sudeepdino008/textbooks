;; exercise 4.25
;; in an applicative-order scheme, the function will run forever.
;; it'll work in normal-order language, as at n=1, the "else" will be evaluated and 1 would be returned, terminating the condition.
